name: portfolio_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'  # Changed to a range instead of ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  # Remove the duplicate cupertino_icons
  cupertino_icons: ^1.0.8  # Keep only one version
  url_launcher: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0  # Changed from 5.0.0 which might be too new

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/